apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cpaas.fullname" . }}-test-connection"
  labels:
    {{- include "cpaas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cpaas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
