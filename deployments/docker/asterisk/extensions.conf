[from-external]
exten => _X.,1,NoOp(Call received from external)
exten => _X.,n,Answer()
; Forward SIP audio to LiveKit SIP Inbound
exten => _X.,n,Dial(PJSIP/${EXTEN}@livekit-sip)
exten => _X.,n,Hangup()
