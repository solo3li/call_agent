[from-kamailio]
exten => _X.,1,NoOp(Call routed from Kamailio)
exten => _X.,n,Answer()
; Forward SIP audio to LiveKit SIP Inbound
exten => _X.,n,Dial(SIP/livekit-sip/${EXTEN})
exten => _X.,n,Hangup()
