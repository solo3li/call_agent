apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpaas.fullname" . }}
  labels:
    {{- include "cpaas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpaas.selectorLabels" . | nindent 4 }}
