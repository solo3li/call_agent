[from-internal]
exten => _X.,1,NoOp(Call from ${CALLERID(num)} to ${EXTEN})
; Here we would route the call to LiveKit SIP Inbound
; For now, we will just play a test message to prove connectivity
exten => _X.,n,Answer()
exten => _X.,n,Playback(hello-world)
exten => _X.,n,Hangup()
